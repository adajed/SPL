int main()
{
    for (n = 2 to 20)
    {
        int i = 2;
        bool isPrime = true;
        while (i * i <= n)
        {
            if (n % i == 0)
                isPrime = false;
            i++;
        }

        if (isPrime)
            printInt(n);
    }

    return 0;
}
