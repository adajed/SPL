int fib(int n)
{
    if (n < 2)
        return 1;
    return fib(n - 2) + fib(n - 1);
}

int main()
{
    int n = 1;
    while (n < 20)
    {
        printInt(fib(n));
        n++;
    }

    return 0;
}
